version: '3.1'

services:
  postgres:
    image: postgres:11.5
    restart: always
    environment:
      - POSTGRES_USER=hakunamatata
      - POSTGRES_DB=hakunamatata
      - POSTGRES_PASSWORD=hakunamatata
    expose:
      - 5432
    ports:
      - 5432:5432
